# Env file for TimescaleDB (timescale/timescaledb)
# Copy to ./secrets/timescaledb.env and adjust values as needed.
# POSTGRES_PASSWORD must be 20-255 characters. Use only ASCII letters/numbers for simplicity.
# This file is the ONLY env_file used by the timescaledb service.

# TimescaleDB connection
POSTGRES_DB=crypto_scout
POSTGRES_USER=crypto_scout_db
POSTGRES_PASSWORD=CHANGEME_ExamplePasswordValue1234567890
TIMESCALEDB_TELEMETRY=off

# Harden local auth on initial cluster creation (requires re-init of the data volume)
# Applies to initdb only; existing clusters won't be changed by setting this later.
POSTGRES_INITDB_ARGS=--auth=scram-sha-256

# TimescaleDB tuning parameters
TIMESCALEDB_TUNE_MAX_CONNECTIONS=100
TIMESCALEDB_TUNE_MAX_BACKGROUND_WORKERS=8
TIMESCALEDB_TUNE_MAX_PARALLEL_WORKERS=4
TIMESCALEDB_TUNE_MEMORY=512MB

# Timezone
TZ=UTC