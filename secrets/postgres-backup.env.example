# Env file for postgres backup sidecar (prodrigestivill/postgres-backup-local)
# Copy to ./secrets/postgres-backup.env and adjust values as needed.
# Credentials (POSTGRES_DB/USER/PASSWORD) may be copied from ./secrets/timescaledb.env if you prefer to keep
# both files independent. Compose loads both env files for the backup service.

# Connection
POSTGRES_HOST=postgres

# Schedule and retention
SCHEDULE=@daily
BACKUP_KEEP_DAYS=7
BACKUP_KEEP_WEEKS=4
BACKUP_KEEP_MONTHS=6

# Dump options
POSTGRES_EXTRA_OPTS=--schema=crypto_scout --blobs

# Optionally copy from ./secrets/timescaledb.env (uncomment and set):
# POSTGRES_DB=crypto_scout
# POSTGRES_USER=sa
# POSTGRES_PASSWORD=change_me
